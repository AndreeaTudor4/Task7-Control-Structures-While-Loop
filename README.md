# Task7-Control-Structures-While-Loop
Calculates the average of the numbers provided by the user. Stops when the user inputs -1

The program continually asks the user to enter a number.
When the user enters "-1" the program stops requesting the user to enter a number.
The program calculates the average of the numbers entered, excluding the "-1".
